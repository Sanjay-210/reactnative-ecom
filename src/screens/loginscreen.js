import { View } from "react-native";
import React from "react";

function loginscreen(){
    return(
        <view>
            <text> 
                loginscreen
            </text>
        </view>
    );

}
export default loginscreen;